
doctype html

html(ng-app="dash")
    head
        base(href="/")
        //- Go through the dependnencies and check all of them 
        //- TODO Shim the external modules into the browserify build? Hmmh.. yeah, why not.
        script(src="/public/bower/angular/angular.min.js" defer async)
        script(src="/public/bower/angular-ui-router/release/angular-ui-router.min.js" defer async)
        script(src="/public/bower/angular-bootstrap/ui-bootstrap-tpls.min.js" defer async)
        script(src="/socket.io.js" defer async)
        script(src="/public/bower/nganalytics/src/ng-analytics.min.js" defer async)
        script(src="/public/js/index.js" defer async)

    body
        //- ng-analytics-auth(label="Hello: &nbsp;" hide-on-auth="true" auth-container="embed-api-auth-container")
        div
            //- Renders the state related HTML here - for example the dashboard.html
            div(ui-view)
