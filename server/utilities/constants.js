"use strict";

module.exports = {
    events: {
        socket: {
            getDasboard: "dash.get.dashboard",
            connected: "connection",
            disconnected: "disconnect"
        }
    }
};